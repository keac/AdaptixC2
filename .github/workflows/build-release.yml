name: Build and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-server:
    name: Build Server (Linux)
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.25.4'

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc g++ make mingw-w64

      - name: Build server
        run: make server

      - name: Build extenders
        run: make extenders

      - name: Package server artifacts
        run: |
          cd dist
          tar -czf ../adaptix-server-linux-amd64.tar.gz *
          cd ..

      - name: Upload server artifact
        uses: actions/upload-artifact@v4
        with:
          name: adaptix-server-linux-amd64
          path: adaptix-server-linux-amd64.tar.gz

  build-client-linux:
    name: Build Client (Linux)
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Qt6 and dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            gcc g++ make cmake \
            libssl-dev \
            qt6-base-dev \
            qt6-base-private-dev \
            libxkbcommon-dev \
            qt6-websockets-dev \
            qt6-declarative-dev

      - name: Build client
        run: make client

      - name: Package client
        run: |
          cd dist
          tar -czf ../adaptix-client-linux-amd64.tar.gz AdaptixClient
          cd ..

      - name: Upload client artifact
        uses: actions/upload-artifact@v4
        with:
          name: adaptix-client-linux-amd64
          path: adaptix-client-linux-amd64.tar.gz

  build-client-macos:
    name: Build Client (macOS)
    runs-on: macos-latest
    permissions:
      contents: read
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          brew install make cmake openssl qt@6

      - name: Build client
        run: |
          export PATH="/opt/homebrew/opt/qt@6/bin:$PATH"
          export CMAKE_PREFIX_PATH="/opt/homebrew/opt/qt@6:/opt/homebrew/opt/openssl"
          export LDFLAGS="-L/opt/homebrew/opt/qt@6/lib -L/opt/homebrew/opt/openssl/lib"
          export CPPFLAGS="-I/opt/homebrew/opt/qt@6/include -I/opt/homebrew/opt/openssl/include"
          export PKG_CONFIG_PATH="/opt/homebrew/opt/qt@6/lib/pkgconfig:/opt/homebrew/opt/openssl/lib/pkgconfig"
          make client

      - name: Package client
        run: |
          cd dist
          tar -czf ../adaptix-client-macos-arm64.tar.gz AdaptixClient
          cd ..

      - name: Upload client artifact
        uses: actions/upload-artifact@v4
        with:
          name: adaptix-client-macos-arm64
          path: adaptix-client-macos-arm64.tar.gz

  build-client-windows:
    name: Build Client (Windows)
    runs-on: windows-latest
    permissions:
      contents: read
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Qt6
        uses: jurplel/install-qt-action@v4
        with:
          version: '6.7.3'
          host: 'windows'
          target: 'desktop'
          arch: 'win64_mingw'
          cache: true

      - name: Build client
        continue-on-error: true
        shell: bash
        run: |
          # Add Qt to PATH (MinGW is included by Qt installer)
          export PATH="${Qt6_DIR}/bin:$PATH"
          cd AdaptixClient
          cmake . -G "MinGW Makefiles" -DCMAKE_BUILD_TYPE=Release
          cmake --build . --parallel
          cd ..
          mkdir -p dist
          if [ -f AdaptixClient/AdaptixClient.exe ]; then
            cp AdaptixClient/AdaptixClient.exe dist/
          elif [ -f AdaptixClient/Release/AdaptixClient.exe ]; then
            cp AdaptixClient/Release/AdaptixClient.exe dist/
          else
            echo "Warning: AdaptixClient.exe not found in expected locations"
            echo "Build may have failed, but continuing to allow artifact upload"
          fi

      - name: Deploy Qt dependencies
        continue-on-error: true
        if: always()
        shell: bash
        run: |
          export PATH="${Qt6_DIR}/bin:$PATH"
          cd dist
          if [ -f AdaptixClient.exe ]; then
            windeployqt.exe --release --no-translations AdaptixClient.exe
          else
            echo "Warning: AdaptixClient.exe not found, skipping Qt deployment"
          fi

      - name: Package client
        if: always()
        shell: bash
        run: |
          cd dist
          if [ "$(ls -A)" ]; then
            7z a ../adaptix-client-windows-amd64.zip *
            echo "Packaged available artifacts"
          else
            echo "Warning: No files in dist directory to package"
            # Create an empty placeholder file to ensure zip is created
            echo "Build failed - no artifacts available" > BUILD_FAILED.txt
            7z a ../adaptix-client-windows-amd64.zip BUILD_FAILED.txt
          fi
          cd ..

      - name: Upload client artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: adaptix-client-windows-amd64
          path: adaptix-client-windows-amd64.zip

  release:
    name: Create Release
    needs: [build-server, build-client-linux, build-client-macos, build-client-windows]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: write
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4.1.3
        with:
          path: artifacts

      - name: Display structure of downloaded files
        run: ls -R artifacts

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            artifacts/adaptix-server-linux-amd64/adaptix-server-linux-amd64.tar.gz
            artifacts/adaptix-client-linux-amd64/adaptix-client-linux-amd64.tar.gz
            artifacts/adaptix-client-macos-arm64/adaptix-client-macos-arm64.tar.gz
            artifacts/adaptix-client-windows-amd64/adaptix-client-windows-amd64.zip
          draft: false
          prerelease: false
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
